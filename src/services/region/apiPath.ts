import { createUrlPath } from "@utils/createUrlPath";

const API_Path = {
  GetAreaDDL: createUrlPath("COM", "GetAreaDDL"),
  GetAreaByAreaNo: createUrlPath("COM", "GetAreaByAreaNo")
};

export default API_Path;
