import styled from "styled-components";

export const TextareaFieldSTY = styled.div`
  .comment-textarea {
    div {
      margin-bottom: 0;
    }
  }

  textarea {
    font-size: 14px;
    resize: none;
  }

  .hint {
    text-align: right;
    font-size: 12px;
  }
`;
