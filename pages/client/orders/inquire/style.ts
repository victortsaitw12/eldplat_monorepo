import styled from "styled-components";

const BodySTY = styled.div`
  max-width: 1030px;
  width: 100%:

  .breadcrumbs {
    width: 100%:
  }
  .page-title {
    color: ${({ theme }) => theme.color.N300};
    font-size: 32px;
    font-weight: 600;
    margin-bottom: 20px;
  }
`
export { BodySTY }