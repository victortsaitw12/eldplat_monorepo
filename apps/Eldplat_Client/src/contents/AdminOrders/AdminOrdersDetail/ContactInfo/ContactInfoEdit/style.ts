import styled from "styled-components";

const BodySTY = styled.div`
  .input-error {
    color: ${({ theme }) => theme.color.R400};
    font-weight: 400;
    font-size: 12px;
    line-height: 16px;
  }
`;

export { BodySTY };
